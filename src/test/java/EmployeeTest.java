import org.testng.Assert;
import org.testng.annotations.Test;
//import task.Employee;
//import task.IsEmployeePageDisplayed;
//import task.Login;

//public class EmployeeTest extends Base {

    //@Test
    //public void testLogin() {
       // Login.as(webDriver, "admin", "admin123");
       // Employee.as(webDriver,"name1", "email", "address", "phone");
        //Employee.as(webDriver, "name", "email", "address", "phone");
       // Assert.assertTrue(IsEmployeePageDisplayed.form(webDriver));
   // }
//}
